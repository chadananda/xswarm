# Security configurations for npm
# Prevent scripts from running automatically during install
ignore-scripts=true

# Use exact versions to prevent unexpected updates
save-exact=true

# Audit packages on install
audit-level=moderate

# Prevent npm from accessing system-wide packages
global-style=true

# Use package-lock for consistent installs
package-lock=true