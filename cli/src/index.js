// xswarm CLI main entry point
import { displayHeader } from './header.js';
import { showPlaceholder } from './placeholder.js';

// Main CLI function
const main = () => {
  displayHeader();
  showPlaceholder();
};

// Run the CLI
main();
