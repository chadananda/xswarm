---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import Button from '../components/Button.astro';
import Card from '../components/Card.astro';
import { getCollection } from 'astro:content';

// Get latest blog posts
const blogPosts = (await getCollection('blog'))
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf())
  .slice(0, 3);
---

<Layout title="XSwarm - Transform Every Developer into an AI Team Leader">
	<Navbar />
	
	<main>
		<!-- Hero Section -->
		<section class="hero relative overflow-hidden terminal-window">
			<!-- Wide Hero Image Background -->
			<div class="absolute inset-0 z-0">
				<img src="/teamofone.png" alt="Cyberpunk developer controlling AI swarm" class="w-full h-full object-cover opacity-30" />
				<div class="absolute inset-0 bg-gradient-to-t from-terminal-bg via-terminal-bg/50 to-transparent"></div>
			</div>
			
			<!-- Terminal Window Effect -->
			<div class="terminal-header">
				<div class="terminal-buttons">
					<span class="terminal-button terminal-button-red"></span>
					<span class="terminal-button terminal-button-yellow"></span>
					<span class="terminal-button terminal-button-green"></span>
				</div>
				<div class="terminal-title">xswarm.ai</div>
			</div>
			
			<div class="container mx-auto px-4 py-24 md:py-32 relative z-10">
				<div class="text-center max-w-4xl mx-auto">
					<h1 class="text-5xl md:text-7xl font-bold mb-6 glitch-text" data-text="Transform Every Developer into an AI Team Leader">
						Transform Every Developer into an <span class="text-gradient-cyber">AI Team Leader</span>
					</h1>
					<p class="text-xl md:text-2xl mb-8 leading-relaxed terminal-text">
						<span class="cli-prompt">Command your own team of AI agents that work in parallel to multiply your impact.</span>
						<span class="block mt-2">From solo coder to team leader—orchestrate, delegate, and deliver at scale.</span>
						<span class="cli-cursor"></span>
					</p>
					<div class="flex flex-col sm:flex-row gap-4 justify-center">
						<Button href="/blog/getting-started-guide" size="lg" class="neon-border-green">
							<span class="terminal-text">./get-started.sh</span>
						</Button>
						<Button href="https://github.com/chadananda/xswarm" variant="outline" size="lg" class="neon-border-cyan">
							<span class="terminal-text">git clone xswarm</span>
						</Button>
					</div>
				</div>
			</div>
			
			<!-- Matrix Rain Effect -->
			<div class="matrix-rain"></div>
		</section>

		<!-- Features Section -->
		<section id="features" class="py-20 terminal-bg">
			<div class="container mx-auto px-4 max-w-6xl">
				<div class="text-center mb-16">
					<h2 class="text-3xl md:text-4xl font-bold mb-4 glitch-text" data-text="Your AI Development Team Awaits Your Leadership">
						Your AI Development Team Awaits Your Leadership
					</h2>
					<p class="text-lg max-w-2xl mx-auto terminal-text">
						<span class="cli-prompt">As team leader, you orchestrate specialized AI agents that handle implementation while you focus on strategy and vision</span>
					</p>
				</div>
				
				<div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
					<Card 
						icon="🏗️" 
						title="Architect Agent" 
						description="Designs system architecture, evaluates trade-offs, and ensures scalability for your projects"
					/>
					<Card 
						icon="💻" 
						title="Developer Agent" 
						description="Writes clean, efficient code following best practices and your project's conventions"
					/>
					<Card 
						icon="🧪" 
						title="Testing Agent" 
						description="Creates comprehensive test suites, identifies edge cases, and ensures code reliability"
					/>
					<Card 
						icon="📝" 
						title="Documentation Agent" 
						description="Maintains clear, up-to-date documentation and generates usage examples"
					/>
					<Card 
						icon="🔍" 
						title="Review Agent" 
						description="Provides instant code reviews with actionable suggestions for improvement"
					/>
					<Card 
						icon="🚀" 
						title="DevOps Agent" 
						description="Handles deployment, monitoring, and infrastructure optimization"
					/>
				</div>
			</div>
		</section>

		<!-- Benefits Section -->
		<section class="py-20 terminal-bg">
			<div class="container mx-auto px-4 max-w-6xl">
				<div class="grid md:grid-cols-2 gap-16 items-center">
					<div>
						<h2 class="text-3xl md:text-4xl font-bold mb-6 glitch-text" data-text="Achieve More with Less Effort">
							Achieve More with Less Effort
						</h2>
						<div class="space-y-6">
							<div class="flex items-start space-x-4">
								<div class="flex-shrink-0 w-12 h-12 bg-blue-100 dark:bg-blue-900/30 rounded-lg flex items-center justify-center">
									<span class="text-2xl">⚡</span>
								</div>
								<div>
									<h3 class="text-xl font-semibold mb-2 terminal-text">10x Productivity</h3>
									<p class="text-terminal-green">Complete complex tasks in minutes instead of hours with parallel AI assistance</p>
								</div>
							</div>
							<div class="flex items-start space-x-4">
								<div class="flex-shrink-0 w-12 h-12 bg-green-100 dark:bg-green-900/30 rounded-lg flex items-center justify-center">
									<span class="text-2xl">🎯</span>
								</div>
								<div>
									<h3 class="text-xl font-semibold mb-2 terminal-text">Consistent Quality</h3>
									<p class="text-terminal-green">Maintain high standards across your codebase with AI-powered reviews and best practices</p>
								</div>
							</div>
							<div class="flex items-start space-x-4">
								<div class="flex-shrink-0 w-12 h-12 bg-purple-100 dark:bg-purple-900/30 rounded-lg flex items-center justify-center">
									<span class="text-2xl">🔄</span>
								</div>
								<div>
									<h3 class="text-xl font-semibold mb-2 terminal-text">Seamless Workflow</h3>
									<p class="text-terminal-green">Integrate AI agents into your existing development process without disruption</p>
								</div>
							</div>
						</div>
					</div>
					<div class="relative">
						<div class="aspect-square terminal-window flex items-center justify-center">
							<div class="text-center">
								<div class="text-6xl font-bold mb-4 glitch-text" data-text="75%">
									<span class="text-gradient-cyber">75%</span>
								</div>
								<p class="text-lg font-semibold terminal-text">
									Less Time on Repetitive Tasks
								</p>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Latest Blog Posts -->
		<section class="py-20 terminal-bg">
			<div class="container mx-auto px-4 max-w-6xl">
				<div class="text-center mb-12">
					<h2 class="text-3xl md:text-4xl font-bold mb-4 glitch-text" data-text="Latest Insights">
						Latest Insights
					</h2>
					<p class="text-lg terminal-text">
						Learn how to maximize your productivity with AI-powered development
					</p>
				</div>
				
				<div class="grid md:grid-cols-3 gap-8 mb-12">
					{blogPosts.map((post) => (
						<article class="terminal-window hover:scale-105 transition-transform duration-300">
							<div class="terminal-header">
								<div class="terminal-buttons">
									<span class="terminal-button terminal-button-red"></span>
									<span class="terminal-button terminal-button-yellow"></span>
									<span class="terminal-button terminal-button-green"></span>
								</div>
								<div class="terminal-title">{post.data.title}</div>
							</div>
							<div class="terminal-content p-6">
								<time class="text-sm text-terminal-cyan">
									{new Intl.DateTimeFormat('en-US', { dateStyle: 'long' }).format(post.data.publishDate)}
								</time>
								<h3 class="text-xl font-semibold mt-2 mb-3">
									<a href={`/blog/${post.slug}`} class="terminal-text hover:text-terminal-cyan transition-colors">
										<span class="cli-prompt">{post.data.title}</span>
									</a>
								</h3>
								<p class="text-terminal-green line-clamp-3">
									{post.data.description}
								</p>
								<Button href={`/blog/${post.slug}`} variant="ghost" size="sm" class="mt-4">
									Read more →
								</Button>
							</div>
						</article>
					))}
				</div>
				
				<div class="text-center">
					<Button href="/blog" variant="outline">
						View All Articles
					</Button>
				</div>
			</div>
		</section>

		<!-- CTA Section -->
		<section class="py-20 terminal-window">
			<div class="container mx-auto px-4 max-w-4xl text-center">
				<h2 class="text-3xl md:text-4xl font-bold mb-6 glitch-text" data-text="Ready to Lead Your Own AI Development Team?">
					Ready to Lead Your Own AI Development Team?
				</h2>
				<p class="text-xl mb-8 terminal-text">
					<span class="cli-prompt">Join the open-source movement transforming developers into AI team leaders</span>
				</p>
				<div class="flex flex-col sm:flex-row gap-4 justify-center">
					<Button href="https://www.npmjs.com/package/xswarm" size="lg" class="neon-border-green">
						<span class="terminal-text">npm install -g xswarm</span>
					</Button>
					<Button href="/blog/getting-started-guide" variant="secondary" size="lg" class="neon-border-cyan">
						<span class="terminal-text">./read-guide.sh</span>
					</Button>
				</div>
			</div>
		</section>
	</main>
	
	<Footer />
</Layout>

<style>
	/* Import terminal colors */
	.terminal-bg {
		background: var(--terminal-bg);
	}
	
	.text-terminal-green {
		color: var(--terminal-green);
	}
	
	.text-terminal-cyan {
		color: var(--terminal-cyan);
	}
	
	.text-terminal-magenta {
		color: var(--terminal-magenta);
	}
	
	.text-gradient-cyber {
		background-image: linear-gradient(45deg, var(--terminal-cyan), var(--terminal-magenta));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		text-shadow: 0 0 40px var(--terminal-cyan);
	}
	
	.line-clamp-3 {
		display: -webkit-box;
		-webkit-line-clamp: 3;
		-webkit-box-orient: vertical;
		overflow: hidden;
	}
	
	@keyframes float {
		0%, 100% {
			transform: translateY(0px);
		}
		50% {
			transform: translateY(-20px);
		}
	}
	
	.animate-float {
		animation: float 6s ease-in-out infinite;
	}
	
	.delay-1000 {
		animation-delay: 1s;
	}
</style>